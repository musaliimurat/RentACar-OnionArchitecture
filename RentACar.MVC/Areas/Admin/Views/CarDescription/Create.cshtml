
@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@model CarDescriptionVM

<div class="card">
    <div class="card-body">

        <h4 class="card-title">Create new car description</h4>

        <form asp-action="Create" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="carId">Select car</label>
                <select class="form-control" id="carId" asp-for="CreateCarDescriptionDto.CarId" asp-items="@(new SelectList(Model.GetAllCarsWithBrandNameForAdminDtos, "Id", "DisplayName"))">
                    <option hidden>Select car</option>
                </select>
                <span asp-validation-for="CreateCarDescriptionDto.CarId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="details">Details</label>
                <input type="text" class="form-control" id="details" asp-for="CreateCarDescriptionDto.Details" placeholder="Enter details">
                <span asp-validation-for="CreateCarDescriptionDto.Details" class="text-danger"></span>

            </div>


            <button type="submit" class="btn btn-success waves-effect waves-light">Create</button>
        </form>
        @section Scripts {
            @{
                await Html.RenderPartialAsync("_ValidationScriptsPartial");
            }
        }
    </div>
</div>

